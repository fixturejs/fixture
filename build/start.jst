(function( root, factory ) {

  // AMD
  if ( typeof define === "function" && define.amd ) {
    define( [], factory );

  // Browser
  } else {
    root.<%= _.str.capitalize(pkg.name) %> = factory();
  }

})(this, function() {
